import SwiftUI

/// Person 2: User login screen.
struct LoginView: View {
    // TODO: Person 2 will implement login with email/password.
    // TODO: Person 2 will store auth tokens securely (Keychain).
    
    var body: some View {
        Text("Login — Person 2 will implement")
            .foregroundColor(.secondary)
    }
}
