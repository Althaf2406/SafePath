import SwiftUI
import Combine

/// Person 3: Offline survival mode showing cached data when network is unavailable.
struct OfflineSurvivalView: View {
    // TODO: Person 3 will implement offline data display using Person 1's cached shelter, route, and alert data.
    // TODO: Person 3 will show last sync time and data freshness indicators.
    var body: some View {
        Text("Offline Survival Mode — Person 3 will implement")
            .foregroundColor(.secondary)
    }
}
