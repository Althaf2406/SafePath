import SwiftUI

/// Person 2: User registration screen.
struct RegisterView: View {
    // TODO: Person 2 will implement user registration with name, email, phone, and password fields.
    // TODO: Person 2 will call backend registration endpoint.
    
    var body: some View {
        Text("Register — Person 2 will implement")
            .foregroundColor(.secondary)
    }
}
