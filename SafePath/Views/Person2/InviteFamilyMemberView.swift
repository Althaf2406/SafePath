import SwiftUI

/// Person 2: Invite a member to a family group.
struct InviteFamilyMemberView: View {
    // TODO: Person 2 will implement invite via phone number, link, or QR code.
    var body: some View {
        Text("Invite Family Member — Person 2 will implement")
            .foregroundColor(.secondary)
    }
}
